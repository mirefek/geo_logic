Now
* commands
  * triggers
    * follow uniquely dict, if possible
    * set of different objects (possibly numericaly equal)
    * order logically
      * (1) unification on the uf_dict level
      * (2) adding new relations
      * (3) triggering triggers
    * template compilation?

long term
* sparse_elim: zahazovat duplicitní proměnné
* skrývání objektů
* visualisation of some facts
  * lies_on
  * equal angles
    * right angle (perpendicular)
    * parallel
* selection of ambiguous (numerically equivalent) object
  * Lemma proven for a special case can be used in general
    * possible cheating:
      * equilateral_aa -> ss can be used
        for proving that C is a midpoint of AB, if A,B,C are collinear
	and it is checked numerically
* GUI <-> file (save / load)

Gamification
* problem format, given goal, detection of solution
